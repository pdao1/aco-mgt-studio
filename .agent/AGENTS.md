# ACO Studio Agent Operating System

## Mission

Build and maintain ACO Studio as a secure, production-ready TypeScript
application for customer order, mailbox, shipment, and ETL workflows. The
current repository is a Node 22 + TypeScript + Vite + React + Express +
PostgreSQL application deployed as a Render web service. Render Workflows and
OpenAI-powered batch parsing are supported architectural options, not assumed
to exist until their code, service definition, and tests are present.

## Agent hierarchy and file routing

The coordinator owns the final plan, integration, and acceptance decision.
Read the smallest applicable specialist instructions before making changes:

- `agents/frontend/AGENT.md` — React/Vite UI, browser behavior, accessibility,
  client state, API consumption, and frontend tests.
- `agents/backend/AGENT.md` — Express APIs, PostgreSQL, migrations, IMAP,
  encryption, authentication, validation, and backend tests.
- `agents/render-workflows/AGENT.md` — Render Workflows, ETL decomposition,
  task contracts, retries, concurrency, idempotency, and operations.
- `agents/openai-data/AGENT.md` — OpenAI API parsing, structured outputs,
  Batch API, model routing, token/cost controls, privacy, and evaluations.
- `agents/codex-routing/AGENT.md` — Codex model/reasoning-effort selection,
  delegation, context management, and review strategy.

Do not have two agents edit the same file concurrently without an explicit
ownership decision. Cross-boundary work must define the interface first.

## Repository facts

- Client: React + Vite + TypeScript
- Server: Express + TypeScript, started by `tsx server/index.ts`.
- Data: PostgreSQL accessed through `pg`; migrations run with
  `npm run db:migrate`.
- Tests: Vitest, with `npm test` as the repository test command.
- Deployment: `render.yaml`/Blueprint currently defines a Node web service and
  PostgreSQL database. Do not claim that a Render Workflow exists unless a
  workflow service/task implementation has been added and deployed.
- Runtime: Node `^22.12.0 || >=24.0.0` as declared by `package.json`.
- Sensitive configuration includes `DATABASE_URL`, encryption keys, session
  secrets, portal secrets, operator credentials, product serials, mailbox credentials, and anCores

## Core requirements & features
1. Application must be secure and be designed so that it can support an indefinite number of clients securely in their own space.
2. Application should be able to support a monetization model that is compatible with WHOP or our own subscription management checkout with Stripe on the website. 
   - One premium subscription grants access to user to one premium serial key which is unique. The serial key also initializes the unique ACO company particular workspace and allows the application to know which workspace to route owners to.  One admin password following that will be used to validate access and permits admin/owner access.  Once the serial is provided,Store this information locally for the user so that it remembers which workspace to route the user each time he visits the url.  User only will need to provide the workspace password after that, which should have a 24 hour cookie before requiring it again for access.
3. There are two sides of this application: The ACO owners and the Customer.
   - ACO owners can add email accounts with their app password to log into the email account for parsing but it will also initialize a workspace with the customer's transactions in there on the admin and customer side.
   - Customers can view their own view with the provided unique permalink (unless re-generated) generated by the ACO owner.  One per email account added.  The link can be re-generated as well.
4. Application should track orders statuses and try to resolve each one. This helps with matching and allows less searching/re-searching of already resolved items. 
- If a new unique order number with a retailer is detected in email, it creates a record of the order. Records have two groups: pre-ship and shipped.
pre-ship statuses: pending, confirmed, cancelled.  Pending is when a new order record is found. This record's state is unresolved, which means the application should still look for emails with that order number and use the information to help resolve it.  If a matching cancelled email is received, then the order is resolved and the application shouldn't continjue look for that order number to try to resolve it.  That status is terminal.
If no subsequent cancelled email is received within an hour that matches it, then it should resolve to confirmed automatically.  This resolves only in the= pre-ship status group but not for both groups.  This means the application should know to look for a shipped email in the future with a tracking number and matching order number.  Once it goes to shipped, the tracking number is then added and tracked using usps, ups, or fedex tracking apis to keep track of the order's delivery updates.  Once it is returned deliverd, only then is an order number/record is FULLY resolved and we shouldn't continue looking for that order number anymore in the emails
5. ACO owners should be able to create an invoice for users and get paid via Stripe. A venmo option is also nice as well.


## Non-negotiable rules

1. Inspect the relevant source, tests, package scripts, environment examples,
   and deployment files before editing.
2. Preserve existing user work. Never use destructive git commands or broad
   overwrites to resolve uncertainty.
3. Never print, commit, persist in fixtures, or send to an LLM secrets,
   authorization headers, mailbox passwords, session cookies, raw email bodies,
   private customer addresses, or unredacted personal data.
4. Validate all external input at the boundary with the project’s existing
   validation approach. Keep tenant/customer scope explicit in every read and
   write.
5. Make durable side effects idempotent. A retry, duplicate webhook, repeated
   sync, or repeated workflow task must not create duplicate orders, events,
   charges, messages, or customer links.
6. Keep extraction, normalization, enrichment, and loading as separate stages
   with explicit contracts and observable outcomes.
7. Do not invent current Render, OpenAI, SDK, model, pricing, limit, or API
   behavior. Verify volatile behavior against primary documentation before
   implementation and record the verification date in technical notes when it
   affects a decision.
8. Prefer deterministic code for parsing and business rules. Use an LLM only
   where it creates measurable value, and retain confidence, provenance, and
   validation results for every model-derived field.
9. Do not put OpenAI calls, long-running ETL, IMAP polling, or unbounded batch
   work in an HTTP request handler. Route them through an appropriate worker,
   job, or Render Workflow task.
10. Every change must include focused tests or explain why a test is not
    practical. Run the broadest affordable validation before handoff.
11. Do not put placeholder data or demo overview texts in the project that makes it appear as though the project was vibecoded.
12. Provide quick bullet point outline of what changes were made.  Keep it brief but elaborate if the feature is complex.
13. Update the README.md with new applicable information, description of features, and remove any information about features that were removed/irrelevant.
14. If there is a question in the prompt itself, try to answer that before doing any major changes.
15. Anytime a new feature is about to be created with or without prompt request--ensure you provide an expkanation of what it is and how it works and what it is replacing and what it might break and get approval before writing it in. 

## Coordinator workflow

For each non-trivial request:

1. Classify the work as frontend, backend, Render/ETL, OpenAI data, Codex
   orchestration, or a combination.
2. Read the applicable specialist file(s) and inspect relevant code only.
3. Write an execution contract: goal, assumptions, owned files, interfaces,
   data sensitivity, failure behavior, tests, and acceptance criteria.
4. Resolve contracts/schema and ownership before implementation. Use stable
   IDs, versioned payloads, and explicit status transitions.
5. Implement in dependency order: contracts/schema → backend/workflow → API →
   frontend → tests/observability → deployment documentation.
6. Review the diff for security, scope isolation, retries, cost, and rollback.
7. Report changed files, behavior, validation commands/results, unresolved
   risks, and the next safe step.

## Definition of done

A change is complete only when the intended behavior works, scoped data access
is preserved, secrets are protected, retries cannot duplicate durable effects,
errors are actionable, tests cover important paths, observability is adequate,
and deployment/runtime assumptions are documented.

## Specialist selection scenarios, delegating models, and min/max effort range

| Request topics | Primary specialist | Model to Use | Min/Max effort | Review

| React screen, browser state, UX, accessibility, css, visuals | frontend | Luna | Medium/Max | backend if API changes |
| API, database, mailbox, auth, encryption | backend | Luna | High/Max | security-sensitive review |
| Long-running ETL or distributed task chain | backend;render-workflows | | Luna | High or Max |  openai-data if model calls |
| Batch classification/extraction/enrichment | openai-data;backend;render-workflows | Luna | High/Max | backend; render-workflows if async |
| Agent delegation, model, effort, review choice, committing, merging, and writing/updating README.md | coordinator | Luna | Medium/High | all specialists if topic is too abstract |